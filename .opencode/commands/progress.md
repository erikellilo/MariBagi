---
description: Where are we — last shipped, next slice, what's blocking
---
Today is: !`date +%Y-%m-%d` ( !`date +%A` )

Read the devlog at @docs/DEVLOG.md.

Tell me, short and chatty:
1. What was last shipped (most recent session log + last checked slice).
2. Which slices are next on the menu (unchecked, in order).
3. The 1 slice that matters most for the next ~90 min.

This is a progress check-in, not a doc. End with one question about what I want to tackle.
